diff --git a/include/configs/egf_3sm1009.h b/include/configs/egf_3sm1009.h
index 427fa5671e..30763721f3 100644
--- a/include/configs/egf_3sm1009.h
+++ b/include/configs/egf_3sm1009.h
@@ -66,6 +66,8 @@
 	"emmc_dev=2\0"\
 	"sd_dev=1\0"
 
+#include "egf_swu.h"
+
 #define CONFIG_UPDATE_PACKAGE_HEADER_LENGTH 36
 #define CONFIG_UPDATE_PACKAGE_LOADADDR 0x44000000 
 #define CONFIG_UPDATE_PACKAGE_INITIAL_LOAD_LENGTH 0x4000064
@@ -97,6 +99,7 @@
 #else
 #define CFG_EXTRA_ENV_SETTINGS		\
 	CFG_MFG_ENV_SETTINGS \
+	CFG_SWU_ENV_SETTINGS \
 	JAILHOUSE_ENV \
 	SR_IR_V2_COMMAND \
 	BOOTENV \
@@ -117,9 +120,9 @@
 	"bootm_size=0x10000000\0" \
 	"mmcdev=2\0" \
 	"mmcpart=1\0" \
-	"mmcroot=/dev/mmcblk2p2 rootwait rw quiet \0" \
+	"set_mmcroot=setenv mmcroot /dev/mmcblk2p${rootfspart} rootwait rw quiet \0" \
 	"mmcautodetect=yes\0" \
-	"mmcargs=setenv bootargs ${jh_clk} ${mcore_clk} console=${console} root=${mmcroot}\0 " \
+	"mmcargs=setenv bootargs ${jh_clk} ${mcore_clk} console=${console} root=${mmcroot} panic=5\0 " \
 	"loadbootscript=fatload mmc ${mmcdev}:${mmcpart} ${loadaddr} ${bsp_script};\0" \
 	"bootscript=echo Running bootscript from mmc ...; " \
 		"source\0" \
@@ -193,6 +196,7 @@
 	"bsp_bootcmd=echo Running BSP bootcmd ...; " \
 		"run usbboot; " \
 		"run usbboot_update; " \
+		"run set_bootchain;" \
 		"mmc dev ${mmcdev}; if mmc rescan; then " \
 		   "if run loadbootscript; then " \
 			   "run bootscript; " \
diff --git a/board/egf/common/mmc.c b/board/egf/common/mmc.c
index 8cd5079f96..006a38afc1 100644
--- a/board/egf/common/mmc.c
+++ b/board/egf/common/mmc.c
@@ -41,8 +41,8 @@ void board_late_mmc_env_init(void)
 	env_set_ulong("mmcdev", dev_no);
 
 	/* Set mmcblk env */
-	sprintf(mmcblk, "/dev/mmcblk%dp2 rootwait rw", mmc_map_to_kernel_blk(dev_no));
-	env_set("mmcroot", mmcblk);
+	sprintf(mmcblk, "setenv mmcroot /dev/mmcblk%dp${rootfspart} rootwait rw", mmc_map_to_kernel_blk(dev_no));
+	env_set("set_mmcroot", mmcblk);
 
 	sprintf(cmd, "mmc dev %d", dev_no);
 	run_command(cmd, 0);
