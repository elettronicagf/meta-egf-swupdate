
rootfs=`swupdate -g`

if [ $rootfs == '/dev/mmcblk2p2' ];then
	selection="-e stable,copy2"
else
	selection="-e stable,copy1"
fi

SWUPDATE_ARGS="-H @BOARDNAME@:@BOARDREV@ ${selection} -f /etc/swupdate.cfg"